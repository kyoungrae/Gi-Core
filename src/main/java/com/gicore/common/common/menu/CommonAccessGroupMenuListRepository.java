package com.gicore.common.common.menu;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CommonAccessGroupMenuListRepository extends JpaRepository<CommonAccessGroupMenuList,String> {
}